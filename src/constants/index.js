const LOCAL_PETS = "localpets";

const LOGIN_DATA = "loginx";

const ROUTES = {
  home: "/",
  dashobard: "/dashboard",
  login: "/login",
  about: "/about",
};

export { LOCAL_PETS, LOGIN_DATA, ROUTES };
