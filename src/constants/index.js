const LOCAL_PETS = "localpets";

const LOGIN_DATA = "loginx";

const ROUTES = {
  home: "/",
  dashobard: "/dashboard",
  login: "/login",
  register:"/register",
  about: "/about",
  services:"/services"
};

export { LOCAL_PETS, LOGIN_DATA, ROUTES };
